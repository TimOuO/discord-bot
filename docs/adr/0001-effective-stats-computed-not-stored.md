# 裝備加成即時計算，不寫入基礎屬性

`User.attack`/`User.defense` 是角色升級累積的基礎屬性；`Item.effectValue` 是武器/防具的加成值。裝備道具時，我們選擇**不**把加成直接加進 `User.attack`/`User.defense`，而是在戰鬥或顯示時，即時把「基礎屬性 + 目前所有已裝備道具的 effectValue」加總算出有效屬性。

這樣換裝、卸裝、裝備互換都只需要增減 `EquippedItem` 記錄，不用擔心忘記把基礎值加回去或扣掉導致資料失真，也能隨時分辨一個角色的數值有多少是練出來的、多少是裝備給的。
